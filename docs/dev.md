# Development

Use Node.js from `.nvmrc`, run `pnpm install`, copy `.env.example` to `.env`, and configure
`config.yaml`. Start watch mode with `pnpm dev`. Run `pnpm check` and `pnpm test:e2e` before
submitting changes; run `pnpm build:lib` when changing public library exports.
