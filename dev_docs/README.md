# Development Notes (`dev_docs/`)

This directory is reserved for working notes, local experiments, design scratchpads, and RFCs during in-progress development.

- Content in this directory is ephemeral and should not be relied upon as production documentation (use `docs/` for permanent documentation).
- Files here are not shipped in container images or production releases.
