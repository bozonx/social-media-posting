# @bozonx/social-posting-platform-catalog

Versioned, browser-safe platform capability profiles backed by official documentation.

Profiles describe validation facts and API availability. They do not register publishing
adapters. A profile marked `restricted` or `unavailable` must not be presented as an enabled
publishing integration.
